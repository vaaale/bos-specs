
## Session 14:00

### 14:00
<!-- session:d64080db-a0f4-45db-be3f-c0ae0744c8e3 turn: transcript:/home/alex/.claude/projects/-home-alex-PycharmProjects-BOS-browseros/d64080db-a0f4-45db-be3f-c0ae0744c8e3.jsonl -->
- User requested Claude Code perform another code review pass, similar to a previous review.
- Claude Code verified branch install behavior, preview process environment variables, and app registration mechanisms by reading `branch-data-root.ts`, `proc.mjs`, and `FrontendToolsV2.tsx`.
- Claude Code identified three real bugs: (1) `branchDataRoot` incorrectly short-circuited on preview role regardless of active branch, causing cross-branch data corruption; (2) app install still called `registerApp`/`launch` on base even for branch-only installs, creating phantom dock entries; (3) bundled-asset conflict records wrote to wrong root and lost merges.
- Claude Code fixed all three bugs by editing `branch-data-root.ts` (removed short-circuit), `store.ts` (prevented registration for branch installs), and `bundledAssets.ts` (rooted conflict queue), then replaced obsolete test with one guarding the fixed bug.
- Claude Code ran `playwright test` on app and full unit suites (430 passed, 19 failed from pre-existing baseline) and `tsc` (clean) and `eslint` (one pre-existing warning in `FrontendToolsV2.tsx`).
- Claude Code updated `bos-system-specs` to add FR-012, FR-013, and SC-007, then committed changes with message "Review pass: fix cross-branch data root and phantom dock entries" (`8c21a4f`).

